---
description: Run the Mobius interview workflow.
---

# Interview

Use the agent's Bash tool to execute `mobius interview`. Preserve any user-provided arguments and append them after the subcommand using normal shell quoting.

```text
Bash('mobius interview [args]')
```
