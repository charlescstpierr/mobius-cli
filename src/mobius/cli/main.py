"""Minimal Mobius CLI bootstrap.

Full Typer command registration is implemented by the next CLI framework
feature; this entry point exists so the package script is importable and
invokable from the initial project skeleton.
"""


def main() -> None:
    """Run the scaffold CLI entry point."""
